import {
  AlertMessage,
  AlertDefault,
  AlertIcon,
  AlertRoot,
  AlertList,
  AlertListItem,
} from './alert';

export const Alert = {
  Root: AlertRoot,
  Icon: AlertIcon,
  Message: AlertMessage,
  Default: AlertDefault,
  List: AlertList,
  ListItem: AlertListItem,
};
